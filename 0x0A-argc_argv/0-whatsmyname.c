#include "main.h"
#include <stdio.h>
/**
 * main - prints its name
 * @agrc: arguments count
 * @argv: arguments vector
 *
 * return: always zero 
 */
int main (int argc, char *argv[]){
       (void) argc;
printf("%s\n", argv[0]);
return (0);}
